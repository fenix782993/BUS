import {NAV} from "../../data/navigation";
import {Sparkles,ChevronRight} from 'lucide-react';
export default function Sidebar({page,setPage,player}){return <aside><div className="logo"><span className="logo-mark">F</span><span>FENIX <b>CITY</b></span></div><div className="sidebar-caption">VIRTUAL CITY · 2.4</div><nav className="sidebar-nav">{NAV.filter(item=>!item.admin||player?.role==='admin'||player?.nickname==='FENIX').map(({id,label,icon:Icon})=><button key={id} className={page===id?"active":""} onClick={()=>setPage(id)}><Icon size={18}/><span>{label}</span>{page===id&&<ChevronRight className="nav-chevron" size={15}/>}</button>)}</nav><div className="sidebar-status"><Sparkles size={16}/><div><b>Город online</b><small>Экономика работает</small></div></div></aside>}
