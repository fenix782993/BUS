import Card from "../common/Card";import ProgressBar from "../common/ProgressBar";
export default function MissionCard({mission,onClaim}){return <Card><div className="mission-top"><span>{mission.period}</span>{mission.completed&&<b>ГОТОВО</b>}</div><h2>{mission.title}</h2><p>{mission.description}</p><ProgressBar value={mission.progress/mission.target*100}/><div className="mission-meta"><span>{mission.progress.toLocaleString()} / {mission.target.toLocaleString()}</span><span>+{mission.xp_reward} XP</span></div><div className="reward">+{mission.cash_reward.toLocaleString()} ₽ · +{mission.coin_reward} FC</div>{mission.completed&&!mission.claimed&&<button onClick={()=>onClaim(mission.id)}>Забрать награду</button>}</Card>}
