import {useEffect,useState} from "react";import PageHeader from "../../components/layout/PageHeader";import Card from "../../components/common/Card";import {get} from "../../api/client";export default function RankingPage(){const[x,setX]=useState([]);useEffect(()=>{get("/api/leaderboard").then(setX)},[]);return <><PageHeader title="Рейтинг"/><Card><div className="leaderboard">{x.map(r=><div className="leader" key={r.id}><b>#{r.rank}</b><span>{r.nickname}</span><span>LVL {r.level}</span><strong>{r.xp} XP</strong></div>)}</div></Card></>}
