import {useEffect,useState} from "react";import PageHeader from "../../components/layout/PageHeader";import {get} from "../../api/client";import CityOverview from "../../components/city/CityOverview";import DistrictCard from "../../components/city/DistrictCard";import EventCard from "../../components/city/EventCard";export default function CityPage(){const[d,setD]=useState([]),[e,setE]=useState([]);useEffect(()=>{get("/api/districts").then(setD);get("/api/events").then(setE)},[]);return <><PageHeader title="Город"/><CityOverview/><div className="section"><div className="section-title"><h2>Районы</h2></div><div className="cards">{d.map(x=><DistrictCard key={x.id} item={x}/>)}</div></div><div className="section"><div className="section-title"><h2>События</h2></div><div className="cards">{e.map(x=><EventCard key={x.id} item={x}/>)}</div></div></>}
