import {useEffect,useState} from 'react';import PageHeader from '../../components/layout/PageHeader';import Card from '../../components/common/Card';import {getAchievements} from '../../api/achievements';
export default function AchievementsPage(){const[data,setData]=useState([]);useEffect(()=>{getAchievements().then(setData)},[]);return <><PageHeader title="Достижения" subtitle="История твоих целей и наград."/><div className="cards">{data.map(x=><Card key={x.id} className={x.unlocked?'achievement unlocked':'achievement locked'}><div className="achievement-icon">{x.unlocked?'✓':'?'}</div><h2>{x.title}</h2><p>{x.description}</p><span>+{x.xp_reward} XP · +{x.coin_reward} FC</span></Card>)}</div></>}
