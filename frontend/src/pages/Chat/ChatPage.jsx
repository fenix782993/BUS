import {useEffect,useState} from 'react';import {MessageCircle,Send,Users,RefreshCw} from 'lucide-react';import PageHeader from '../../components/layout/PageHeader';import Card from '../../components/common/Card';import {get,post} from '../../api/client';
export default function ChatPage(){const[messages,setMessages]=useState([]),[text,setText]=useState(''),[busy,setBusy]=useState(false);async function load(){try{setMessages(await get('/api/social/chat'))}catch{}}useEffect(()=>{load();const t=setInterval(load,4000);return()=>clearInterval(t)},[]);async function send(e){e.preventDefault();if(!text.trim())return;setBusy(true);try{await post('/api/social/chat',{text:text.trim()});setText('');load()}catch(e){alert(e.message)}finally{setBusy(false)}}return <><PageHeader title="FENIX CHAT" subtitle="Городской чат — общайся, находи игроков и договаривайся о сделках." action="Обновить" onAction={load}/><Card className="chat-shell"><div className="chat-head"><div><span className="eyebrow">LIVE CITY CHAT</span><h2><MessageCircle/> FENIX CITY</h2></div><span className="live-dot">● ONLINE</span></div><div className="chat-feed">{messages.map(m=><div className={m.own?'chat-msg own':'chat-msg'} key={m.id}><div className="chat-avatar">{m.sender.slice(0,1).toUpperCase()}</div><div><b>{m.sender}</b><p>{m.text}</p><small>{new Date(m.created_at).toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'})}</small></div></div>)}</div><form className="chat-compose" onSubmit={send}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Напиши сообщение..." maxLength={1000}/><button disabled={busy}><Send size={18}/></button></form></Card><div className="social-cards"><Card><Users/><b>Игроки</b><strong>LIVE</strong><p>Находи партнёров для торговли, бизнеса и семей.</p></Card><Card><RefreshCw/><b>Автообновление</b><strong>4 сек</strong><p>Новые сообщения появляются автоматически.</p></Card></div></>}
