import {useEffect,useState} from "react";import PageHeader from "../../components/layout/PageHeader";import Card from "../../components/common/Card";import {get} from "../../api/client";export default function BusinessPage(){const[p,setP]=useState([]);useEffect(()=>{get("/api/properties").then(setP)},[]);return <><PageHeader title="Бизнес"/><div className="cards">{p.map(x=><Card key={x.id}><small>{x.district}</small><h2>{x.name}</h2><p>Доход: {x.income.toLocaleString()} ₽ / период</p><strong>{x.price.toLocaleString()} ₽</strong></Card>)}</div></>}
