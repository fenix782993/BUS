import {useEffect,useState} from "react";import PageHeader from "../../components/layout/PageHeader";import VipCard from "../../components/donations/VipCard";import {get} from "../../api/client";export default function VipPage(){const[x,setX]=useState([]);useEffect(()=>{get("/api/vip").then(d=>setX(d.tiers))},[]);return <><PageHeader title="VIP"/><div className="cards">{x.map(t=><VipCard key={t.name} tier={t}/>)}</div></>}
