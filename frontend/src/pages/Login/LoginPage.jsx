import {useState} from 'react';import {login,register} from '../../api/auth';
export default function LoginPage({onLogin}){const[mode,setMode]=useState('login'),[nickname,setNickname]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{const d=mode==='login'?await login(nickname,password):await register(nickname,password);localStorage.setItem('fenix_token',d.token);onLogin(d.player)}catch(e){setError(e.message)}finally{setBusy(false)}}return <div className="login"><div className="loginbox"><div className="login-mark">F</div><small>FENIX CITY</small><h1>{mode==='login'?'С возвращением':'Создай персонажа'}</h1><p>Город, экономика, работа, автомобили и твой прогресс.</p><form onSubmit={submit}><input value={nickname} onChange={e=>setNickname(e.target.value)} placeholder="Никнейм" minLength="3" maxLength="32" required/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Пароль" type="password" minLength="4" required/><button disabled={busy}>{busy?'Подключение…':mode==='login'?'Войти':'Создать аккаунт'}</button></form>{error&&<div className="error">{error}</div>}<button className="switch" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Нет аккаунта? Регистрация':'Уже есть аккаунт? Войти'}</button></div></div>}
