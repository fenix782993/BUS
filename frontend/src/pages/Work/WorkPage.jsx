import {useState} from 'react';import {BriefcaseBusiness,Clock,Zap} from 'lucide-react';import PageHeader from '../../components/layout/PageHeader';import Card from '../../components/common/Card';import {work,rest} from '../../api/player';
export default function WorkPage({player,refresh}){const[msg,setMsg]=useState('');async function doWork(){try{const d=await work(player.id);setMsg(`Работа выполнена: +${d.reward.toLocaleString('ru-RU')} ₽`);refresh()}catch(e){setMsg(e.message)}}async function doRest(){try{await rest(player.id);setMsg('Энергия восстановлена');refresh()}catch(e){setMsg(e.message)}}return <><PageHeader title="Работа" subtitle="Зарабатывай деньги и опыт."/><div className="two"><Card className="work-card"><BriefcaseBusiness size={34}/><h2>Городская смена</h2><p>За работу ты получаешь ₽, XP и репутацию. Одна смена расходует 10 энергии.</p><div className="work-stats"><span><Zap/> {player.energy}%</span><span><Clock/> сейчас</span></div><button disabled={player.energy<10} onClick={doWork}>Выйти на смену</button></Card><Card><h2>Отдых</h2><p>Восстанови 35 энергии и получи небольшой бонус XP.</p><div className="energy-big">{player.energy}%</div><button onClick={doRest}>Отдохнуть</button></Card></div>{msg&&<div className="notice">{msg}</div>}</>}
