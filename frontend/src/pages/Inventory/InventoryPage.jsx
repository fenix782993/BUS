import {useEffect,useState} from "react";import PageHeader from "../../components/layout/PageHeader";import Card from "../../components/common/Card";import {getInventory} from "../../api/shop";export default function InventoryPage(){const[x,setX]=useState([]);useEffect(()=>{getInventory().then(setX)},[]);return <><PageHeader title="Инвентарь"/><Card>{x.length?x.map(i=><div className="row" key={i.id}><span>Предмет #{i.item_id}</span><b>x{i.quantity}</b></div>):<p>Инвентарь пуст.</p>}</Card></>}
